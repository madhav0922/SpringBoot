spring.application.name=NoBSSpringBoot
spring.datasource.url=jdbc:mysql://localhost:3306/nobs
spring.datasource.username=root
spring.datasource.password=mysql@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Lecture 14: Exception Handling - Part 1 - Built in Spring Exception Handling
# included this to have the message always, which automatically includes the message in the HTTP response,
# we were already throwing in the RunTimeException.
server.error.include-message=ALWAYS
# Included this to automatically include a stack trace (exactly same as the one on console) in the HTTP response.
server.error.include-stacktrace=ALWAYS

